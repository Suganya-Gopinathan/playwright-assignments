const newbrowserVersion = "Chrome";

function getBrowserVersion() {

    if (newbrowserVersion === "Chrome") {
        let newbrowserVersion = "Safari";
        console.log("The browser version is " + newbrowserVersion);
    }
    else {
        //var newbrowserVersion = "Chromium";cl
        console.log("The browser version is " + newbrowserVersion);
    }
    console.log("The browser version is " + newbrowserVersion);
}

getBrowserVersion();
console.log("The browser version is " + newbrowserVersion);
